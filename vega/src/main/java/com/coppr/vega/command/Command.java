package com.coppr.vega.command;

/**
 * Represents a command that can be handled by a {@link CommandHandler}.
 *
 * <p>A command encapsulates the input or intent required to perform an
 * operation. Implementations should contain the data required by their
 * corresponding command handler.</p>
 */
public interface Command {
}